<fieldset class="mar10">
<% @user = User.find(comment.user_id)%>
	<legend><%= @user.username %></legend> 
	<%= simple_format comment.body %>
  	<br/><em>Posted on <%= comment.created_at.strftime('%b %d, %Y at %I:%M %p') %></em>
</fieldset>