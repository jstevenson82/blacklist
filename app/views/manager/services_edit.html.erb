<h1>Editing service</h1>

<%= form_for @service, :url => manager_update_services_path( @service ) do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <input type="hidden" name="service[b_id]" value="<%= @business.id %>">
  
  <div class="pad10">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="pad10">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions pad10">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', services_manager_path(@business.id) %>
