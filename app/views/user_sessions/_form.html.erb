<%= form_for(@user_session) do |f| %>
  <% if @user_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_session.errors.count, "error") %> prohibited this user_session from being saved:</h2>

      <ul>
      <% @user_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div>username</div>	
	<div><%= f.text_field :username, :size => 20 %></div>
	<div>password</div>
	<div><%= f.password_field :password, :size => 20 %></div>
	  <div class="actions">
	    <%= f.submit "Log in" %>
	  </div>
	<div>Not registered? Signup <%= link_to "here", new_user_path%></div>

<% end %>
