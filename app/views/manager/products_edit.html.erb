<h1>Editing product</h1>

<%= form_for @product, :url => manager_update_products_path( @product ) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="pad10">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="pad10">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="pad10">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
  
  <input type="hidden" name="product[b_id]" value="<%= @business.id %>">
  
  <div class="actions pad10">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', products_manager_path(@business.id) %>
